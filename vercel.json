{
  "version": 2,
  "buildCommand": "pnpm install && PORT=3000 BASE_PATH=/ pnpm --filter @workspace/exampro run build && pnpm --filter @workspace/api-server run build:vercel",
  "outputDirectory": "artifacts/exampro/dist/public",
  "installCommand": "pnpm install",
  "functions": {
    "api/index.js": {
      "memory": 1024,
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
